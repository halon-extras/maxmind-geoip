[
    {
        "name": "geoip",
        "parameters": {
            "required": [
                {
                    "name": "ip",
                    "type": "string"
                }
            ]
        },
        "returnType": "array",
        "detail": "geoip(string $ip): array",
        "value": "geoip($0)",
        "documentation": "Lookup an IP address with MaxMind's GeoIP2 database. Returns the country name `country` (string) and country ISO code `iso_code` (string) as an associative array. An empty array will be returned if no data is available for the queried IP. Upon error the function will return an associative array with a `error` (string) property.",
        "link": "[Full documentation](https://github.com/halon-extras/maxmind-geoip)"
    }
]